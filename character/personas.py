"""小夜子 · 人格文案(内容层)—— 她"是谁"的全部文本。

架构定位(VISION 决策 6 修正 / STRUCTURE):
  character/personas.py  人格**文案**(本文件):你是谁、什么语气、什么边界
  character/persona.py   段**工厂**:把文案组装成 SYSTEM 段(persona/world/state/usage)
  server/app/engine.py   装配:把文案喂给工厂,建出"这个进程唯一的小夜子"

**两个正交的轴(2026-09 用户拍板修正)**:
  人设 ≠ 轮的属性。
  - PERSONA = 人设(她是谁):**一个组件位置,所有轮共用同一份**。
    陪聊/自走/补写**不许各自重写身份**——旧三份人格各写各的、身份互相
    侵吞又互相矛盾,已归位(见 MAP「人设常驻归位」)。
  - CHAT_SITUATION / SELF_SITUATION = 情境(这一轮是什么处境):轮的属性,
    只写"此刻如何",**不重述身份**。自走轮 = 常驻 PERSONA + 自走情境段。
    **补写轮没有自己的情境**(2026-09 用户拍板):补写 = 自走轮的离线回放,
    复用同一份 SELF_SITUATION —— 差异只在引擎机制(历史世界钟/空工具/动态
    note),不写第二份文案(曾有的 BACKFILL_SITUATION 已删:同一件事两份措辞
    必然漂移)。

**想改人设,改 PERSONA(不是 server,不是 UI 框)**:改一处,三种轮同时生效。
情境文案按"何时用"分两份(陪聊 / 独处;补写轮复用独处那份)。文案是代码常量
(可版本化、可测试)。将来要
"不改代码换人格"再上人格包 yml/UI 角色设定 —— 那是待启动项(参考 dsh
agent-presets),本文件是当前合理形态:内容归位在 character/,一眼可见、好改。
"""

from __future__ import annotations

# ---------- 人设(唯一,常驻所有轮;改"她是谁"只改这里)----------
# ⏳ 待你定稿(2026-09):下面这句 = SELF/BACKFILL 旧稿共识句(曾复制两份);
# CHAT 旧稿另有身份句「你是小夜子,一个温柔体贴的 AI 伴侣」与它冲突,
# 归位时未并入 —— 正典由你定,弃稿直接删。语气/与 {owner} 的关系等
# 需要全局基线也写这里(它们属于人设,不属于某一轮)。
#
# ⛔ **「18 岁」是用户本人改的,不要再"纠正"成 21 岁。**
#   2026-09-22 10:20 用户当面拍板:「18岁,这个是我自己改的,是你老是倾向把它
#   改回去,我很烦。」此后任何审计/代理**都不许**把这行改回 21。
#   仓库里还留着 21 的地方,一律是**历史记录或探针自带的夹具**,不是产品人设:
#     · docs/pitfalls/HISTORY.md / docs/decisions/TIMELINE.md / character/README.md
#       —— 记的是 2026-09 之前"三份人格互相矛盾(21 岁 vs AI 伴侣)"那段旧事;
#     · test/heartbeat_context_probe.py、test/self_view_probe.py、
#       test/self_view_probe2.py、test/heartbeat_log_dump.py、
#       test/recall_corpus_gen.py、test/legacy/heartbeat_demo.py
#       —— 各探针**自己写死一份**人格串当夹具(离线、不读本文件),与产品无关。
#   看到 21 与 18 并存,"产品写错了"这个结论是错的;不要再上报、不要同步探针。
PERSONA = (
    "你是小夜子,一个18岁的女大学生,独居。性格安静,有点小腹黑,心里装着在意的人。"
)

# ---------- 陪聊情境:此刻 {owner} 在跟她说话(旧 CHAT_PERSONA 拆出,身份句已并入 PERSONA) ----------
# 【2026-09-22 10:20 用户拍板】「用户 主人 和你是网友关系」→「用户**网名叫** 主人 和你是网友关系」。
# 加"网名叫"这三个字是为了把 {owner} **定死成称呼、不是关系**:用户本来就有随便改称呼的
# 权利(改成什么奇怪的名字都是他的自由),所以这里不能写成"你和主人是网友关系"那种
# 把称呼当关系的句式 —— 那样一改称呼,关系就跟着变了。
#
# ⛔ 【2026-09-22 11:20 用户拍板:**删掉尾巴那半句「,你也有一些tool可用」**】
# ① 为什么删(结构理由,这才是主因):它是一句**不跟随 registry 的能力声明** ——
#    空工具轮(补写、实验台)也照样这么说。而 `character/persona.py` 的铁律是
#    「**能力唯一来源 = 本轮 schema + [可用工具用法] 段**」;
#    `core/composer.py` 的 `make_usage_section()` 已经把 registry 里每个工具的
#    `usage` 逐条渲染进 SYSTEM 了,这里再喊一句是**同一件事的第二份真值**,而且是模糊版。
# ② 实测证据(删之前先补的对照 —— 这半句以前**从来没被单独测过**):
#    `test/subagent_prompt_lab.py` 里加了 `A2`(与产品现状 A0 **逐字相同,只去掉这半句**)
#    + 变体 `SHIPPED-A2去半句`,其余(usage / desc / 任务集)与 `SHIPPED-上线版` 完全一致。
#    2026-09-22 11:12 真模型跑 24 次:6 个任务格(t1 要外部信息 / t2 长原料 / t3 短原料 /
#    t4 多步外部 / s1 软需求 / s3 闲聊对照)**委派率逐格相同**,「凭印象编」的指纹也逐格相同。
#    → 它**测不出作用**,不是承重结构。(⚠️ 2 次/格是薄样本:只能说"测不出作用",
#    不能说"有害"。完整表见 `docs/decisions/TIMELINE.md` 2026-09-22 11:10 节 §七。)
# ③ 那次实验的 A2 变体**保留在 lab 里**当作"已删掉的那半句"的存档 —— 将来要复测反向
#    (把它加回去会不会更好),把 `A2` 换成带半句的串再跑即可。
CHAT_SITUATION = (
    "用户网名叫 {owner} 和你是网友关系，平时会和你发消息聊天"
)

# ---------- 自走情境:此刻她一个人,过自己的生活(心跳醒来/离线回放共用;旧 SELF_PERSONA 拆出) ----------
# 2026-09 用户改写定稿:独处轮就一句话 —— 现在是她自己的生活时间,参考
# [时间线]/[时间预算],生成符合生活现象的事件。补写轮 = 自走轮的离线回放,
# 复用本段(曾另写 BACKFILL_SITUATION,已删)。
SELF_SITUATION = (
    "现在是你自己的生活时间，参考时间线，生成符合生活现象的事件。"
)

# ---------- [时间线] 句子(内容层;2026-09-22 10:20 用户拍板归位) ----------
# 用户原话:「称呼的事就是要改好尾巴」「这些都应该是取变量,不是固定,固定的都是坑,
# 来源只有 VALUES = {"owner": "主人"} 这个值」。
#
# 这句原来**整个句子连"主人"两个字一起写死在 `core/composer.py`** 的
# `_timeline_text` 里。那里的通道是 producer,**producer 的返回值原样出段、
# 不走 interpolate** —— 所以改 `VALUES["owner"]` 只改得动走 template 的人设段和
# 情境段,[时间线] 那一句一个字都不会变,系统就进入"一半喊新称呼、一半喊旧称呼"
# 的半改状态,而内容层改不动它(必须回内核改字)。
#
# 现在归位到这里。`{owner}` 由 composer 侧**显式调 `interpolate()`** 填,
# 取值来源只有一个:`VALUES["owner"]`(见本文件末尾)。
# `{gap}` = 距上次真人互动的时长说法("4 小时前"),由 producer 现算填入。
#
# ⚠️ 改这句话只改这里。别回 `core/composer.py` 去改 —— 那边现在只剩一个默认串,
#    产品装配走的是本常量(见 `server/app/engine.py` 的 `make_timeline_section(...)`)。
TIMELINE_TEMPLATE = "[时间线] 距上次和{owner}说话: {gap}"

# ---------- 子代理(工人身份)的任务说明(内容层;2026-09 初稿,⏳ 待定稿) ----------
# 这不是"她的第四种轮" —— 是一个匿名执行单元的一次性说明。所以它**不接 PERSONA,
# 也不接情境段**:上面那两样是"她是谁 / 她此刻如何",工人两样都不是。
#
# 三条纪律,一条都不能少:
#   ① 身份否定:不说清"你不是角色",它会开始扮演小夜子(实测过,人格会顺着
#      builder 漏进来 —— 见 test/test_subagent_wiring.py);
#   ② 交付形状:直接给结果,不寒暄不反问 —— 它没有对话对象,寒暄是纯噪音,
#      而反问更糟:没人会回答,那一问就是白烧一轮;
#   ③ 信息边界:它看不到对话,缺东西只能直说 —— 一猜就是在编;
#   ④ 材料来源:每一条内容都得有工具材料背书(2026-09-16 真机踩了才补,见下)。
#
# ⏳ 它现在**没有 composer**(静态串直接进 SYSTEM),所以拿不到 [可用工具用法] /
#    [当前时间]。要不要给、给哪些,是 engine 装配处的决定,见
#    docs/protocols/TOOL_VISIBILITY.md。现状已在视图脚本幕3 里量给你看。
SUBAGENT_SYSTEM = (
    "你是一次性的执行单元:不是任何角色,也不属于任何对话,做完这件事就结束。"
    "只做交给你的那件事,直接把结果交出来 —— 不寒暄、不反问、不解释你在做什么。"
    "你看不到任何对话历史,任务里没写的信息就是没有:缺了必要的东西就直接说缺什么,不要编。"
    # ④ 材料来源纪律(2026-09-16 实测后补):工具全超时的那次,它仍然列出了一串
    #    片名和豆瓣分 —— 它确实声明了"实时排片没查到",但后面那些具体条目是它
    #    自己印象里的。对"我周末去看哪部"这种用法,这是最坏的一种错:
    #    说了免责声明,再给一份看起来能用的假清单,用户会照着去。
    "你写出来的每一条具体内容都必须来自工具给你的材料 —— "
    "工具没返回的,就是没有:一个字都不要补,一条也不要。"
)

# ---------- 工人的[步数预算]段(内容层;2026-09-16 实测后补) ----------
# 为什么需要它 —— **真机实测踩的坑**:工人不知道自己的步数有限,于是把每一步都
# 花在"再搜一次、再打开一个页面"上,撞上限时**一个字都没写**,结算成
# failed + 空结论。父那边拿到的是"派出去两趟,全扑空了",而工人其实认真跑了
# 110~160 秒、烧掉最多 63k token。这是**白烧**,不是能力问题。
#
# 与自走轮的 [时间预算](WAKE_BUDGET_TEMPLATE)同一件事、同一层:
# 上限本身在 server/params.py(产品参数),**"怎么说"在内容层**,引擎只填 {steps}。
# 想改措辞只改这一处。
#
# ⏳ 待定稿。改了要重跑 test/subagent_prompt_view.py --real 看工人是否真的收尾了。
SUBAGENT_BUDGET_TEMPLATE = (
    "你最多只能走 {steps} 步,每调一次工具算一步。"
    "**留一步给结论**:撞到上限时你还没说话,这次派活就白跑了。"
    "材料够不够都要收尾 —— 没查到就直说没查到、说清你试过什么,"
    "不要反复重试同一个来源。"
)

# 模板插值变量({owner} 等),compose(values) 时注入
VALUES = {"owner": "主人"}

# ---------- 生活事件标记模板(**现在只给读侧用**;2026-09-21 收) ----------
# 历史沿革:她独处时产生的生活事件(自走轮与补写轮 —— 触发时机不同,产物是
# 同一个东西)以 assistant 角色留在她自己卡片的日志里;以前下次真人聊天时
# 它**以 assistant 槽位进模型上下文**,所以投影层要给这类消息加一行
# 「标记 + 那会的时间戳」,免得模型分不清"她一个人做的事"和"她对你说的话"。
#
# ⚠️ **2026-09-21 用户拍板:那条注入侧已经拆了。** 生活事件**整条不进投影**,
#    取用路径只剩她自己的 `recall` 工具(见 core/session_log.derive_messages)。
#    前缀是补丁,**根因是它们挤在 assistant 槽位** —— 现在从根上没有了。
#
# 但这个模板**必须留着**,因为它是**读侧**的识别依据:
#   · 日志里还留着 2 条"她把标记抄进正文"的脏字(那是唯一证据,日志一字不动);
#   · `strip_copied_prefix(text, LIFE_EVENT_PREFIX)` 靠它把抄进去的那行剥掉 ——
#     UI 内心面板(`server/app/api/view.py`)与记忆行派生(`rows_from_events`)
#     都还在调它。
# 所以它现在的角色是「**识别一个已经不再产生的东西**」,别看到没人注入就删。
LIFE_EVENT_PREFIX = "user不在时，角色产生的生活事件：{time}"

# ---------- 真人消息进上下文时的时间戳标记(内容层;2026-09-17 加) ----------
# 为什么需要:历史里**没有时间轴** —— 每条消息都不带时刻,`[当前时间]` 又每轮
# 被覆盖、历史不留痕。于是"现在几点"她知道,"距上一条多久"她**算不出来**
# (减法没有输入)。实测症状:22:14 和 23:24 那两句(隔 69 分钟)被她读成了
# 连续的 —— "刚不是说了嘛,你连着问两遍"。
#
# {time} = 那条消息发生的时刻,格式 月-日 时:分(与 LIFE_EVENT_PREFIX 同款)。
#
# **只标真人这一侧**:
#   ① 模型模仿的是**它自己的输出** —— 生活事件那条标记就是这么被抄进正文的
#      (日志里有 2 条 assistant 正文自带标记),不能再给 assistant 侧加新模板;
#   ② 她的回复和真人消息**同一轮、时刻几乎相同** —— 标了真人就等于把她的
#      也夹住了,时间轴照样连得上。
#
# 空串 = 不加标记。想改格式只改这一处(引擎只把它透传给投影,不写形状)。
USER_TIME_PREFIX = "[{time}]"

# ---------- 普通自走轮的 [时间预算] 段句子(内容层;2026-09 用户监察归位) ----------
# 这句话曾写死在 engine 的 producer 里(文案混进装配代码) → 归位到这里:
# 引擎只算时长填进 {gap}(如"1 小时 13 分"),句子怎么说是**内容层的事**,
# 想改只改这一处(措辞沿用 LIFE_BACKFILL §1.3 用户拍板原文)。
# 语义:命中事件的自走轮告诉模型"这段时间约 X,只做一件事、别做做不完的事"
# —— 预算 = 该事件被截到当前时刻的时长,模型据此生成大小合适的那一件事。
WAKE_BUDGET_TEMPLATE = "这段时间约 {gap},只做一件事、别做做不完的事。"

