# Yona 2.0 愿景与技术路线(VISION)

> 规划先行:先定"要成为什么",再映射到技术。本文件是路线图的上游。
> 相关:`MAP.md`(进度)、`D:\MyProject\Yona-架构会诊.md`(旧病诊断)

---

## 一、愿景(2026-09-02 锁定)

| 维度 | 定论 |
|---|---|
| 形态 | **两层**:通用 agent 运行时内核 + 旗舰「小夜子」跑在内核上 |
| 对象 | 发布给别人 + 求职展示作品 |
| 核心体验 | 工具调用 + **后台生命循环(自主)** |
| 重点 | **assistant 输出与工具调用的可观测性**(不是前端) |
| UI | 本地 Web,前端几乎不做;FastAPI thin router |
| 未选≠砍 | RAG/预设/语音/评测:保留,**走"待启动轨道"**,不优先、不大刀阔斧 |
| 成功标准 | 内核可独立测试(回放/恢复/打断)、架构讲得清、API 层瘦身 |

## 二、关键决策(已锁定)

1. **能力 ≈ 工具**:循环很薄,真正写的东西 = 工具 + 上下文装配 + prompt。
   "写好一个工具" = schema + 实现 + 用法散文 + 健壮性 + 出场时机。
2. **autonomy = 后台生命循环,不是定时器**:
   进程活着她就活着——后台心跳(`source=self`)醒来 → 低成本闸门判断
   (时间/条件/随机)→ 值得才让 LLM 决策 → 调**同一套工具**执行。
   用户触发与自我触发共用一套工具 = 同一内核、两个触发源。
3. **可观测性优先**:先让人看清"每一步输出什么、调哪个工具、结果如何"。
4. **会话模型**:本地单用户最小会话(发布给别人后续再加隔离)。
5. **工具痕迹 = 视图,不是日志**:
   - 日志原文全保留(SessionLog 是真相源,可回溯);折叠只发生在"喂给模型的输入视图"层。
   - 默认忠实回放(业界主流 + 协议要求成对回放);可选"折叠视图":已结束轮次里
     非保真工具的痕迹折叠(assistant 剔 tool-call 块、tool/result 跳过),当前轮与
     `retain_result=True` 工具(如 subagent 委派、不可重查查询)全留。
   - 对照业界:Anthropic Context Editing 是手动逃生舱(省 token),不是自动折叠;
     我们 = 自动规则 + 日志保底;动态工具集(RAG 召回 top-k)下价值最大。
6. **两个触发源 = 同一内核(source 参数)**:
   - `run_turn(..., source="user"|"self")`;turn/start 事件带 `source`(审计筛选用)。
   - 自走轮(source=self)**没有真人用户消息**,user 槽放**协议占位串**
     (如"【自动轮】此为 xx 触发的自我轮次,无用户自主消息"),明示模型这轮没真人说话,
     不被当成指令。
   - **自走轮需要自己的上下文视图**:独处的小夜子 ≠ 陪聊的小夜子。
     两套 SYSTEM(不同 composer 段组合:自走轮换 self_persona + 时刻/上次她做了什么)。
     ⚠️ 具体结构未定:旁白/占位 user 是否互相啰嗦、各自保留到什么粒度,留待
     **垂直场景**下用真模型验证再定,不在图纸上空想。
7. **后台生命循环(细化自决策 2)**:
   - 心跳醒来 → **低成本闸门(纯规则:冷却/时段/随机,零 LLM)** → 值得才让 LLM 决策。
     闸门做成可插拔接口;subagent 增强 = 未来倾向,不做。
   - **同一时刻只跑一个 turn**(角色一次只能做一件事):用户消息来时若后台轮还在跑,
     "她在上课没空看手机"是合理设定 —— 不是缺陷。
   - **运行状态可查询**(供 UI 提示):thinking(计划未生成,显示"她正想着…")→
     doing(计划已定在跑,显示"她正在 [当前动作] ,请稍等")→ done。
     当前动作从事件日志投影,不另存状态。
   - **重启补齐**:进程关闭很久后再启动 → 检查日志尾部时间,超阈值就触发一次
     补齐轮(source=self),"她补上错过的时间",提示复用 doing 态。
   - 间隔抖动/启动延迟/前台让步:借鉴旧 Yona autonomy 调度骨架(会诊冻结区,仅搬调度)。
   - 并发两层:turn 级(上面,互斥)+ 工具级(同轮多 tool-call 并行 = dsh executionMode,
     未来项,当前串行执行够用)。
8. **世界 section:系统本地能知道的,注入;不知道的,才配工具**:
   - 三类信息三分:静态死信息(生日/所在地,section 死值)、动态死信息(时间/日期/星期,
     每轮刷新注入的 section)、动作能力(换衣/搜索/发消息,才是工具)。
   - **时间 = 动态死信息,不是工具**:get_time 退役,改由世界 section 每轮现取注入,
     模型随时看得到,不需要"调工具查"。系统每轮刷新一次(世界 tick)。
   - 判断标准一句话:系统本地能知道的(时间/日期)→ 注入;系统不知道的外部信息
     (天气/新闻)→ 才配查询工具。
   - 动机:自走轮探针暴露"场景时刻 vs get_time 返回真实时间"两个时间源打架;
     时刻只来自世界 section(系统真实时间)+ 闸门在真实时刻触发场景,单时间源,不打架。
   - **绝对时间 vs 相对时间分工**:世界 section = 绝对时间(时钟,死信息);
     会话时间线 section(`make_timeline_section`)= 相对时间(距上次真人互动多久,
     从日志 Event.time 派生,不是额外状态)——模型靠后者区分"刚聊完"vs"久别"。
   - 后续所有取舍沿袭此标准。
9. **surface 层:一切"视图操作" = 追加注解事件,日志永不改**(UI 删除/撤回/编辑的落点):
   - 事实:UI 的删除/撤回/编辑**不是删数据**——没有数据库可删;UI 操作 = 向内核发请求,
     内核往 append-only 日志追加一条**注解事件**(给目标事件打 tag:"这句先不用了"),
     它只影响**本会话后续的视图拼装**(投影),日志原文一字不动。
   - 机制:新增注解事件类型 `surface/shadow {start, end, reason}`。投影(derive_messages)
     先按注解事件算出被遮蔽 seq 集合,再生成模型消息面;被遮蔽的旧事件仍在日志里
     (真相完整:她确实说过),只是模型面与 UI 显示按遮蔽过滤。
   - 三态:current(模型当前可见)/ shadowed(被遮蔽,留在日志)/ log-only(纯注解,从不是表面)。
   - 统一:删除/撤回/编辑/未来压缩 全是一个动作 = "遮蔽旧段 + (可选)追加新内容",
     与决策 5(折叠=视图非日志)同一哲学;审计=日志里多一条注解(谁在何时遮蔽了什么)。
   - 参照业界:dsh `core/session/src/surface.ts` —— surfaceOp(append / replace 范围)、
     foldSurface 重放、shadowedSeqs、sourceEventSeqs(血缘:新替旧可追踪)、
     UI 层 ConversationContextOriginKind = compaction | rewind | rewrite;
     codex 的 Esc-Esc fork、Claude Code 的 checkpoint /rewind 同类。
     结论:**UI 需求最终落成内核一等公民原语(surface),而非数据库删除** —— 这是收束口原则
     (外部需求必须能以一等公民形式回落到内核,否则说明需求没想透或该拒绝)。

## 三、技术路线(按优先级)

```
Layer 1 · 内核(yona-rewrite/core)
  ✅ SessionLog          事件源(append/replay/derive)
  ✅ AgentLoop           响应式单循环(工具驱动)
  ✅ Tool/Registry       工具三件套(schema+func+usage)
  ✅ OpenAICompatibleLLM 厂商无关客户端 + .env 配置
  ⏳ P1 可观测性:turn 回放查看器(输出 + 工具调用可视化)
  ⏳ P2 SYSTEM 装配:注册表 + 变量插值(决定"这轮给哪些工具")
  ⏳ P3 后台生命循环:heartbeat + source=self(同一套工具)
  ⏳ P4 最小会话模型(本地单用户)

Layer 2 · 旗舰(小夜子)
  ⏳ 工具集:搜索 / 时间 / 文件 / (记忆:RAG 待启动轨道)
  ⏳ 生命行为:后台该做点什么(基于 P3)
  ⏳ 最小 Web:FastAPI thin router(<200 行)+ 极简前端
```

## 四、待启动轨道(用户 2026-09 定论:不移植,只留注释参考)

RAG 长期记忆(向量重排序)/ 角色扮演预设 / 语音感官(ASR/TTS/vision)/ 质量评测(eval)
—— 旧 Yona 的价值 = 参考(会诊/好资产/踩坑),不是仿照;**不做移植、不贴旧胶水**;
上不上线看它在当前认知闭环里站不站得住。逐项决策与状态见 `STRUCTURE.md` §2。
