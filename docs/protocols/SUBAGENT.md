# 子代理(SubAgent)协议 —— 工人身份的 loop 复用

> 状态:✅ 2026-09-15 对齐身份 / 存在意义 / 复用范围 / 装配策略;
> ✅ 2026-09-16 代码已落地(执行器进 `core/subrun.py`,接线进 `server/app/engine.py`);
> ✅ **2026-09-21 §3.3「划分线」定案** —— 四条轴 + 量化判据;`recall` 归她、工人不是她的超集。
> 相关:`docs/decisions/VISION.md` 决策 1(能力≈工具)、6/7(两个触发源 / 后台生命循环)、
> `docs/decisions/DESIGN.md`(SYSTEM 装配与 source)、`docs/tasks/OPEN.md`。
> 代码落点现状见 §9;提示词怎么试出来的见 §4.3 与 `test/subagent_prompt_lab.py`。

---

## 0. 一页速览

**一句话**:子代理 = **工人身份的 loop 复用**。不是第二个她,是一个由她派出去、
不带人格、会死、不进主日志的一次性执行单元。

**三条存在意义**:隔离 · 并发 · 调度。

**一条硬约束**:`core/loop.py` 的 turn 锁**不可重入** —— 所以只能复用 loop 的
**构造与装配**,不能复用 loop 的**实例**。详见 §2.3。

**哪件活归谁**:见 §3.3 的划分线(四条轴 + 量化判据)。一句话 ——
**她的记忆与自身状态归她,外面的公开信息归工人**,两条轴不重叠,所以工人工具集
**不是**她的超集,也不需要工具之间互相路由。

---

## 1. 身份:工人,不是第二个她

| | 谁在跑 | 日志 | 人格 | 生命周期 | 谁触发 |
|---|---|---|---|---|---|
| 陪聊轮 | **她** | 主日志 | 有 | 活着 | 真人 |
| 自走轮 | **她** | **主日志** | **有**(+自走情境段) | **活着** | 时间 + 闸门 |
| **子运行** | **工人** | **独立日志** | **无** | **会死** | 她的工具调用 |

**子运行明确不是这些**:

- 不是第二个她 —— 拿不到 persona,拿不到 life,不参与心跳/闸门/自走;
- 不写主日志 —— 主日志只留 `tool/call` + `tool/result`(带 `run_id` 血缘);
- 不会做梦 —— 跑完即止,没有"活着"这个状态。

> ⚠️ 为什么身份不能松:dsh 的子代理是"**继承父的预设**,再叠加可选的人格遮盖"
> (`child-agent.d.ts`:`join its parent's preset … then apply the child's own
> shadowing persona section`)。那条路对通用 agent 成立,对陪伴体不成立 ——
> 一旦分身也有她的人格,主日志里"她说的话"和"分身说的话"就分不开了,
> 而"她是一个人"是 VISION 决策 6/7 的根。

---

## 2. 复用范围(写死:复用什么,不复用什么)

### 2.1 复用

| 复用对象 | 说明 |
|---|---|
| **循环代码** | `core/loop.py` 的 `AgentLoop` —— 逐字复用,不写第二个循环 |
| **日志原语** | `core/session_log.py` 的 `SessionLog` —— 每个子运行一份新的 |
| **工具原语** | `core/tools.py` 的 `Tool` / `ToolRegistry` —— 同一套实现,白名单不同 |
| **LLM 客户端** | 同一个 `LLM` 协议 —— 换个实例就换了档位(弱模型实验的接口) |
| **装配机制** | `core/composer.py` 的段装配 —— 按 `source` 路由到不同装配(§4) |

### 2.2 不复用

| 不复用对象 | 原因 |
|---|---|
| **loop 实例** | turn 锁不可重入,见 §2.3 |
| **主日志** | 隔离的全部意义所在 —— 脑内过程不进她的记忆 |
| **persona / 情境段** | 它不是她 |
| **turn 锁** | 她一次只做一件事;**工人不占这条不变量** |
| **life / 心跳 / 闸门** | 它不会活着 |

### 2.3 硬约束:必须新实例

```python
# core/loop.py
self._turn_lock = threading.Lock()      # 不可重入
def run_turn(...):
    with self._turn_lock:               # ← 工具执行发生在这把锁**之内**
        return self._run_turn_locked(...)
```

工具调用是在 `_run_turn_locked` 内部发生的,也就是**已经握着锁**。
此时在工具里再调同一个实例的 `run_turn` = **当场死锁**。

→ **每个子运行一个新的 `AgentLoop` 实例**,共享 llm / composer builder / 工具实现。
装配复用了,锁不撞。

### 2.4 为什么"单次 LLM 就能做的活"也要用 agentloop

这是本设计的**预设立场**(用户 2026-09-15 原话:「虽然这些任务可能单独调用一次
llm 就能完成了用不上 agentloop,但同样用 agent 完成是因为架构正确」)。

买到的不是"更强的能力",而是**同一套基础设施**:

- 同一套 loop、同一套装配路由、同一套预算、同一套日志与可观测;
- 任务哪天变复杂(要多步、要工具、要重试),**不用换范式**;
- 只有一个执行原语要维护,而不是"轻任务走这里、重任务走那里"两套。

---

## 3. 存在意义:隔离 · 并发 · 调度

### 3.1 隔离(三层)

| 层 | 隔什么 |
|---|---|
| 人格 | 子运行拿不到 persona,只有任务级 SYSTEM |
| 日志 | 它的全过程不进她的日志;主日志只有两行 |
| 工具 | 白名单 —— 没给它的工具**从它的 prompt 里消失**,也拒绝执行 |

### 3.2 并发 + 规模

**一次可以发出去多个子代理** —— 这是它作为独立原语(而不是"同步函数调用")
的核心价值,直接推出**队列**:排队 / 并发上限 / 状态 / 取消。

⚠️ **规模问题是真的**:并发意味着成本与故障面同时放大。没有累计预算之前,
"一次发出去很多个"是一句空话。见 §4.3。

### 3.3 调度:主 loop 保持专注

用户 2026-09-15 原话:「它可以切分掉大部分的 tool 不在主 loop 明文显现,
主 loop 对话的时候能够非常专注,需要 tool 的时候就打开 subagent,
subagent 代它去看 tool。当然主 loop 不是完全不留 tool 了。」

**划分线 ✅ 2026-09-21 定案**(此前是本文件的推断,标着"待确认"):

原来那张三行表(**本次已删**)是**第一版推断,信息不全** —— 它只有"上下文"这一条轴,所以
`recall`(只读、要好几步、结果是原料)按它会**整行落在"派出去"那一列**,
而主产品语义说它必须留在她手里。冲突的根因是:那张表把**四条独立的轴**压成了一条。

#### 四条轴(每条独立回答一个问题)

| 轴 | 问什么 | 留在她手里 | 派出去 |
|---|---|---|---|
| **上下文轴** | 中间过程值不值得丢弃 | 小(结论 ≈ 原料) | 大(几万字网页 → 一句话) |
| **收敛轴** | 一步到位,还是探索 N 步才收敛 | 一步 | 多步真收敛 |
| **能力轴** | 她手上有没有这件工具 | 有 | 没有 |
| **归属轴** | **这个事实是谁的** | **她的(记忆 / 自身状态)** | **外面的(公开信息)** |

**通用 agent 的标准答案只覆盖前两条轴。** 它们判"要不要派"只看上下文经济学 ——
中间过程是噪音就隔离掉(Claude Code 的子代理文档、Anthropic 的工具设计文都是这个理由)。
那两条对陪伴体**不全**,后两条才是本产品与通用 agent 的分水岭。

#### 量化判据(可查)

> **派出去 ⟺ (中间过程的体量) ≫ (结论的体量),或者有 N ≥ 2 件互相独立的活可并行。**

**比值 ≈ 1 的活永远不该派** —— 付一整份额外 SYSTEM + 额外一步 + 序列化,
换回的是同一坨字,还多绕一层。

这条**能解释**已有实测(不是新实验,出处 `character/tools.py` 的 `make_launch_subagent_tool()` 里那段「三轮真模型实验…共 126 次真调用」注释):
"她做不了的活" 64/64 派(比值 ∞);"她做得完的长活" 6/52 —— **那正是比值接近 1 的一格**,
所以换任何措辞都推不动。那不是文案失败,是那一格本来就不该有偏好。

#### "多步"有两种,长得一样但不该同样处理

- **收敛型多步**(上网查):每一步拿到新信息、方向在收窄、中间过程可丢弃 → **该派**。
- **重试型多步**(`recall` 实测最多 4 步):没找着 → 换个说法再试。**每一步她都要亲眼看到
  上一步的结果**,才知道下一步怎么写 → **不能派**。把这段交出去,她只拿回一句结论,
  就失去了"我的手感" —— 而灰区 / 四态那套置信度语义,服务的正是这份手感。

#### 归属轴的决定性一条:工人没有记忆

`recall` 查的表按卡与人格范围切分;而工人按定义**无人格、无 life**(§1)。
要让它能查,就得给它同一份记忆访问权 —— 那等于亲手拆掉 §3.1 的"人格层隔离"。

→ 所以 `recall` **不是** `launch_subagent` 的一名成员。它不是"应该"归她,是**只能**归她。
同理,改她自身变量的工具(`change_outfit` 一类、以及将来的健康 / 好感度)同样只能在她手里。

#### 不做嵌套:能力归属唯一

**工人工具集不许是她的超集。** 同一个能力有两个家,模型就必须回答"这件事**在哪做**",
而这个问题的正确答案**不存在**(两边都对),只能比成本 —— 而模型看不见成本。
她的决定应该永远只有"**用不用**",从来没有"**在哪用**"。

这条同时说明:`TOOL_VISIBILITY.md` §5 的"加通道"(在自己这一层注册工具)**有前提** ——
先由本节定完"这件能力归谁",才轮到注册它。

**现状符合这条**:网络两件只注册进工人(`engine._worker_tools`),她手上**一件网络工具都没有**,
所以"她自己也能查天气"这个前提今天不成立,没有重复。

#### 同一个 step 混着出现时:不路由、不代转

直接调用与委派同时出现,**两个都跑,各用各的结果**(`core/loop.py` 同 step ≥2 调用走线程池,
两个结果都会回来)。多花一次调用是可接受的代价 —— 不要让它变成工具之间的路由。三条理由:

1. **工具结果会撒谎。** 若 `recall` 的返回里写「已全权交给 subagent」,它写的不再是事实,
   而是**关于另一个工具的转述**。这与本项目的根(日志即真相)冲突 ——
   同 §4.1 为 `source` 必须独立取值写的那条。
2. **工具之间开始互相知道对方存在。** 2 个工具 1 对耦合,5 个工具 20 对,而且改动面互相锁死。
3. **要治"多余的那一次调用",治在文案上,不是机制上。** 已有实测:
   `docs/decisions/RECALL_MVP.md` §4 洞B —— 天气那一格靠 `recall` 的边界句
   + 本工具那句「缺关键条件先问」,**20% → 90%**。那是"两条规矩各管一段",
   不是"一个工具替另一个路由"。

**行业参照**(说明本文件为什么不能照抄通用 agent 的划分):
dsh 的子代理是"**继承父的预设**,再叠加人格遮盖"(`child-agent.d.ts`);
那条路对通用 agent 成立,对陪伴体不成立,理由见 §1。

### 3.4 能力预期与规模边界(明确不做什么)

**我们不做完整的一套 subagent。** 别人的子代理能扛多步复杂任务,因为它们的
loop 基础设施本身就能循环得很好;我们的 loop 现在很薄,一轮往往一两个 step 就结束。

**本设计的定位**:子代理专门用于**简单的 LLM 任务 + 多个 LLM 任务并发** ——
总结一篇文章、websearch 完再整理格式、其它零散小任务。

**明确不做**(⏸ 挂起):

- 子运行再派子运行(不做递归,就不需要深度守卫);
- 持久化子会话 / 冷恢复;
- 复杂多步编排 / 工作流。

---

## 4. 装配策略

### 4.1 `source` 是路由键

装配不是"新机制",是**加一个 `source` 值**。`core/loop.py` 的 SYSTEM builder 签名
本来就是 `(registry, source, log)`(arity 2/3),`server/app/engine.py` 已经按
`source` 决定 persona 与时间源 —— **加一个分支,隔离的设置点就现成了**。

```
source="user"       → 旗舰装配(persona + 情境 + 世界 + 状态 + 工具用法,**无 [时间线]**)
source="self"       → 旗舰装配 + 自走情境段 + 时间预算(同一份 persona)
source="subagent"   → 任务级 SYSTEM,无 persona、无情境段、白名单工具   ← 本协议新增
                       ← **⏳ 未落地**(今天的实现是 `core/subrun.py` 的 `execute()` 里那句
                         `loop.run_turn(spec.task, source="user", **overrides)` 的 `source="user"`
                         + 静态 `system_prompt=`,见 `test/test_subagent_wiring.py` 的现状测试)
```

> **【2026-09-23 00:01 分账:这一行藏着两件事,只解决了一件】**
>
> 上面那句"静态 `system_prompt=`"里藏着**两个独立的问题**,五天里一直被当成一件事:
>
> | # | 问题 | 状态 |
> |---|---|---|
> | 1 | **装配** —— 工人没走 composer,于是 `[可用工具用法]` 从来没到过它手上 | ✅ **已解(甲A,用户拍板)**:`SubRunSpec.system` 放宽成 `str \| Callable`,`engine` 传**工人自己的 builder**(`character/persona.py` 的 `build_worker_composer`)。**段清单与主循环同一套机制,只是清单不同** |
> | 2 | **`source` 正名** —— 工人轮在日志里记的是 `"user"`,与真人聊天轮分不开 | ⏳ **仍未解**,归 `docs/protocols/TOOL_VISIBILITY.md` §4 的"判定键"那条待拍 |
>
> **为什么走甲A 而不是"加一个 `source="subagent"` 分支"**(后者才是本行原本的写法):
> 两者行为差异在**失败形态** —— 甲A 里工人那台 composer **根本没有 persona 段**,
> "工人穿上人设"**物理上不可能**;走分支则依赖路由写对,而路由一旦写错,失败形态
> 恰好是 `df0d72f` 当时极力避免的那种(**工人穿上人设、用她的语气干活,而且没有症状**)。
> **装配与 `source` 正名不绑在一起做。**

> 【2026-09-21 23:15 更正】`source="user"` 那一行当时写错了:陪聊轮早就不挂 `[时间线]`。
> —— 真相:`server/app/engine.py` 里那句注释「陪聊轮 = 主人正在跟她说话:只给世界时刻
> (`[当前时间]`),**不挂 [时间线]**」,
> `chat_composer` 没传 `extra_sections`;`[时间线]` 只挂在 `self_composer` /
> `backfill_composer`(那两处 `extra_sections=[timeline_section, wake_budget_section]`);
> `character/persona.py` 的 `build_small_night_composer()` 证明默认装配 = persona + situation + world + state + usage。
>
> 【2026-09-21 23:15 标 · ⏳ 未落地】上面 `source="subagent"` 那一行写得像是**已经在装配里了**,
> 其实**没有这个值** —— 真相:`core/subrun.py` 的 `execute()` 里是
> `loop.run_turn(spec.task, source="user", **overrides)`;全仓库 `.py` grep 不到
> `source="subagent"`;`test/test_subagent_wiring.py` 的测试名就叫
> `test_subrun_turn_is_indistinguishable_from_a_chat_turn`,断言
> `starts[0]["data"]["source"] == "user"`。(该行立的"不许复用 `user`"规矩**照旧成立**。)

**`source` 必须是它自己的值,不许复用 `"user"`。** 理由:

1. 本项目的核心是**日志即真相**,`source` 存在的全部意义就是记来源。库里已有
   三个先例:`self`(自走)、`compact`(摘要顶替)、`user-edit`(编辑);
   `subagent` 是第四个,不给值才是例外。
2. 实打实的后果:子运行自己的日志若标 `"user"`,`make_timeline_section`
   (`core/composer.py`:判 `source == "user"`)会算出"距上次真人互动 X 分钟"
   —— **那是句谎话**。

**`source` 的现成消费点**(加新值时必须一起看):

| 位置 | 用途 |
|---|---|
| `core/loop.py` `run_turn` | 写进 `turn/start` 与 `user/message` |
| `core/loop.py` | `source == "self"` → user 槽放占位串 |
| `core/subrun.py` 的每轮 `system_prompt=` 覆盖 | **今天的真落点**;`core/loop.py` 的 `_system_text` 路由是**目标形态** ⏳ |
| `core/session_log.py` | `self_turns` 投影 / 已结束轮跳过自走占位 / **已结束独处轮的 assistant 整条不进投影** / 独处轮**不占** `last_turns` 名额 |
| `core/composer.py` | 时间线的"最后一次真人互动"锚点 |
| `server/store.py` | 会话列表 / life 流的过滤 |

> 【2026-09-21 23:15 更正】上表 `core/loop.py` `_system_text` 那一行当时写错了:它**不是今天的落点**。
> —— 真相:`core/subrun.py` 的 `execute()` 里 `loop = AgentLoop(sub_log, llm, registry,
> system_prompt=spec.system, ...)` 把任务级 SYSTEM 当**静态 `system_prompt=`** 传进 `AgentLoop`,
> 不经 `_system_text`(函数定义在 `core/loop.py` 的 `_system_text`,它只对 **callable** 才路由)。
>
> 【2026-09-21 23:15 更正】上表 source 消费点清单当时不全,漏了 `source="self"` 现在最重的两个后果。
> —— 真相:`core/session_log.py` 的 `derive_messages()` 里那句
> `if data.get("turn") in self_turns and data.get("turn") in ended_turns: continue`
> (已结束独处轮的 assistant **整条不进投影**)、
> 以及同函数的 `ordered_ended = sorted(t for t in ended_turns if t not in self_turns)`
> (窗口只数真人对白轮,见该函数 docstring「独处轮(自走 / 补写)不占这个名额」);
> 正典 `docs/decisions/TIMELINE.md`「2026-09-21 · 记忆检索接进主链路 + 往事不常驻」§一(用户拍板)。

### 4.2 该看到什么 / 不准看到什么

> **【2026-09-23 00:01 更正 + 补全】** 下面这张表原来是**五行**,而且**漏了最关键的一行**。
> 后果是实打实的:实现照这张表核对,"工具用法段"因为**表里没有这一行**而从未被核对,
> 于是工人整整五天**拿不到 `[可用工具用法]`** —— 拿到的是 `description`(走 `tools[]`,
> 与 SYSTEM 无关),所以表现得像模像样、**没有任何症状**。
> 根因、取证与裁决见 `docs/decisions/TIMELINE.md`「2026-09-23 00:01 · 工人 SYSTEM 曾经是
> "一条静态串"」。**教训:`该看到什么` 这张表必须与 composer 实际注册的段清单对账**,
> 漏一行不会有任何报错。

| 项 | 给不给 |
|---|---|
| persona | ❌ |
| 她的状态段(穿着等) | ❌ |
| 世界段(时钟等动态死信息;含世界设定) | ❌ **已拍(2026-09-23 00:01)** —— 用户:「**可不止有时间**,很多甚至类似于小说奇幻设定的东西都有可能进去,所以**不用待定了,噪音太大,不进 subagent**」。原来是 ⏳ 待定 |
| 时间线段(距上次真人互动) | ❌(对工人无意义) |
| 任务书 | ✅(进它的 user 槽) |
| 工具 | ✅ 白名单(空 = 无工具) |
| **工具用法段(`[可用工具用法]`)** | ✅ **已拍(2026-09-23 00:01)** —— 用户:「**可见工具肯定是要补进去的**」。⚠️ **这一行是补上的,原表没有**;它**必须跟随本轮注册表**渲染(白名单变了,用法段跟着变),所以工人需要**自己的 composer**,不能是一条静态串 |

**"给不给"的判据(2026-09-23 补)**:凡是**跟随 registry 或轮次状态动态生成**的段,
必须经 composer 渲染 —— 用静态串表达 = **表达力降级**,差集会自己长出来且**零症状**。


### 4.3 预算(**2026-09-15 实测,这是本设计最硬的一条**)

**事实**:`server/params.py` 的 `LLM_OUTPUT_MAX_TOKENS = 4096` 是**拍给聊天轮的**,
且它作用在**每一次调用**上;**产品里没有任何累计预算**(全仓库无实现)。
推理模型的 `reasoning_tokens` **计入** `output_tokens`。

> 【2026-09-21 23:15 更正】上面「4096 作用在**每一次调用**上」这句对**子运行**已经过时
> —— 真相:`server/params.py` 的 `SUBAGENT_OUTPUT_MAX_TOKENS` 已把子运行的单次预算**独立成一档**
> (现值 8192,标 ⏳ 的只是**取值**),并已接线
> `server/app/engine.py` 的 `_run_worker` 里 `max_tokens=SUBAGENT_OUTPUT_MAX_TOKENS`;
> 引擎注释(`_build_engine` 里那句)写着「4096 是拍给聊天轮的…由 `_run_worker` 每轮覆盖」。
> 而「**产品里没有任何累计预算**」这半句**仍然成立**(grep `累计预算` 只命中注释:
> `core/subrun.py` 模块头那句「单次调用上限 vs 累计预算」、
> `test/lab/scheduler.py` 模块头「没有重试、没有优先级、没有截止时间、没有累计预算」那句)。

**实测**(2026-09-15,`deepseek-v4-flash` / `deepseek-v4-pro`,同一"整理长文本"任务):

| 预算 | 模型 | 结果 | 输出 | 其中推理 |
|---|---|---|---|---|
| 4096 | flash | **failed / max-tokens,正文 0 token** | 4093 | 4093 = **100%** |
| 8192 | flash | completed(29.9s) | 6184 | 5522 = 89% |
| 8192 | pro | completed(146.1s) | 6784 | 6353 = 94% |

**结论**:4096 对这个活**不是"紧",是"不可能"**;而"输出预算"实际上几乎
全是**思考预算**。

**已定**:单次预算独立成一档 `server/params.py` 的 `SUBAGENT_OUTPUT_MAX_TOKENS`
(现值 8192,已接线 `server/app/engine.py` 的 `_run_worker` 里 `max_tokens=SUBAGENT_OUTPUT_MAX_TOKENS`)。
**⏳ 待拍**:这个取值本身;以及累计预算(按活 / 按轮 / 按天)设不设、设多少
(目前只有注释,无实现)。

> 【2026-09-21 23:15 标 · ⏳ 待拍】原行把**已定**和**待拍**混成了一句 ——
> 「要不要独立成档(不借聊天轮的值)?」**已经答了**(就是上面这一档),真正待拍的只剩
> **取值本身**与**累计预算**。

### 4.4 工具策略

"无人看守时哪些工具可见" —— 自走轮和子运行**共用一套策略**,不各写各的。

**形态已对齐(2026-09-15)**:见 [`TOOL_VISIBILITY.md`](./TOOL_VISIBILITY.md) ——
两层结构(全局 → 轮次种类)、两条正交轴(能力轴 / 角色轴)、
**减走禁令·加走注册**、深度只来自嵌套执行体(本协议不做递归 ⇒ 恒为两层)。

⏳ 仍待拍:四个轮次种类**各自放哪些工具**(见 `TOOL_VISIBILITY.md` §4)。
⏳ 另见:`TOOL_VISIBILITY.md` §3 实测发现 —— **自走轮与补写轮共用 `source="self"`**,
所以判定键不能只用 `source`,必须派生出「轮次种类」。

---

## 5. 与自走的关系:平行,不是包含

用户 2026-09-15 提问:「这个设计下是不是它的等级又高一层,之前的自走周期
是不是也被它囊括并且需要收束进去了?」

**答:不能并入,但能共享三层。**

- **不能并入** —— 自走是**她在跑**(她的身份、她的日志、她的记忆),子运行不是她。
  把自走并进子代理 = 把"她活着"降级成一个工人,正面砍在 VISION 决策 6/7 的根上。
- **能共享**:

| 层 | 现状(两套) | 应收束为 |
|---|---|---|
| 调度 / 准入 | 闸门决定"起不起自走轮";队列准入决定"起不起子运行" | 同一件事:**小成本规则决定要不要起活** |
| 预算 | 自走用**时间预算**(分钟,只为叙述);子运行要 **token 预算** | 一套统一的预算原语 |
| 工具策略 | 各自为政 | 一套"无人看守时的工具可见性"策略 |

**另一条新性质(⏳ 待异步落地后才成立)**:异步子运行开了之后,"她一次只做一件事"这条不变量**只管她,
不管工人** —— 系统第一次出现"她在说话的同时,旁边有别的活在跑"。这不是冲突,是设计。
(今天 `engine._run_worker` 仍是阻塞语义,"她一次只做一件事"这条不变量暂时还在全量生效)

> 【2026-09-21 23:15 标 · ⏳ 未落地】上面这条性质写成现在时,读起来像**已经发生** ——
> 真相:`server/app/engine.py` 的 `_run_worker` docstring 写着「**阻塞**是 v1 的语义:
> 她这一轮会等它跑完」;异步投递 = 本文件 §7 任务栏的任务 5,未开工。

---

## 6. 拍板记录

| 日期 | 用户原话 / 要点 | 标记 | 影响 |
|---|---|---|---|
| 2026-09-15 | 「subagent 是个工人身份的 loop 复用」 | ✅ | 身份定死:无人格、独立日志、会死。§1 §2 |
| 2026-09-15 | 「它的存在意义是上面说的隔离、并发、调度,并且涉及规模问题」 | ✅ | §3 的骨架;规模 = 必须有累计预算 |
| 2026-09-15 | 「虽然这些任务可能单独调用一次 llm 就能完成了用不上 agentloop,但同样用 agent 完成是因为架构正确」 | ✅ | §2.4;定位 = 简单 LLM 任务 + 并发,不做完整 subagent |
| 2026-09-15 | 「它可以切分掉大部分的 tool 不在主 loop 明文显现…… subagent 代它去看 tool。当然主 loop 不是完全不留 tool 了」 | ✅ | §3.3;具体划分线当时是 AI 推断 —— **已于 2026-09-21 定案,见下行** |
| 2026-09-15 | 提问「自走周期是不是也被它囊括并且需要收束进去了?」→ 用户对"不并入、共享三层"回"行,那就对齐好了" | ✅ | §5 |
| 2026-09-15 | 认可本窗范围(6 条任务,砍掉深度守卫) | ✅ | §7 |
| **2026-09-21** | 「工具应该怎么区分这些结论写进文档吧」—— 认可四条轴(上下文 / 收敛 / 能力 / **归属**)+ 量化判据,并否掉两件:(a) `recall` 归入 `launch_subagent`;(b) 工具之间互相路由(如 `recall` 回「已全权交给 subagent」) | ✅ | **§3.3 定案**(原三行表已删);连带在 `TOOL_VISIBILITY.md` §5 加"加通道的前提" |

---

## 7. 任务栏(可微调)

> **微调规则**:本栏后续会改。改的时候**保留原行、追加变更日期**,不要重写历史行
> —— 日期就是为此留的。
> 状态用:未开工 / 进行中 / 完成 / 挂起。

| # | 任务 | 状态 | 登记日期 | 最近变更 | 依赖 |
|---|---|---|---|---|---|
| 1 | `source="subagent"` + 装配路由(隔离的执行点) | **拆成两半:装配路由 ✅ 完成 / `source` 正名 ⏳ 未开工** | 2026-09-15 | **2026-09-23 00:01**:装配路由已落(用户拍板"甲A" —— `SubRunSpec.system` 放宽成 `str \| Callable` + `character/persona.py` 的 `build_worker_composer()` + `engine._worker_system`)。**没走**本行原写的"加一个 `source` 值然后 `sys_by_source` 分支",理由见 §4.1 那条分账。`source` 正名仍归 `TOOL_VISIBILITY.md` §4 的判定键 | — |
| 2 | 新实例而非共享实例(turn 锁不可重入) | 完成 | 2026-09-15 | 2026-09-16 已落(`core/subrun.py` 的 `execute()` 里 `loop = AgentLoop(...)`) | — |
| 3 | 单次预算 ✅ 已落(8192,`params.py` 的 `SUBAGENT_OUTPUT_MAX_TOKENS` + `engine.py` 的 `_run_worker` 里 `max_tokens=`) | 进行中 | 2026-09-15 | — | 待拍取值 |
| 4 | 队列:并发上限(批量并发是正经用法) | 未开工 | 2026-09-15 | — | — |
| 5 | 异步:投递策略 + **消息级来源**(≠ `turn/start.source`) | 未开工 | 2026-09-15 | — | 4 |
| 6 | 失败契约(空结论怎么表达,别静默空回) | 未开工 | 2026-09-15 | — | — |

> 【2026-09-21 23:15 更正】#2 / #3 的状态当时写错了(与 §9 和代码三方打架:#2 已经落了快一周
> 还在写"未开工") —— 真相:#2 `core/subrun.py` 的 `execute()` 每次都新建 `AgentLoop`
> (`loop = AgentLoop(sub_log, llm, registry, ...)`)
> (该文件模块头注释明说这是硬约束),本文件 §9 自己写着「执行器(**已毕业进内核**)」,
> 测试 `test/test_subagent_wiring.py::test_same_instance_inside_a_tool_deadlocks`
> 在跑;#3 单次预算已落(`server/params.py` 的 `SUBAGENT_OUTPUT_MAX_TOKENS` +
> `server/app/engine.py` 的 `_run_worker` 里 `max_tokens=`;
> `_run_worker` 里还用 `personas_mod.SUBAGENT_BUDGET_TEMPLATE.format(steps=SUBAGENT_MAX_STEPS)` 填 `[步数预算]` 段),只有累计预算没做。

**已砍**(2026-09-15):深度守卫 —— 不做递归就不需要守。

---

## 8. 明确挂起(本窗不碰)

弱模型档位 · 重试 · 优先级 · 截止时间 · UI 面板 · 子会话持久化 / 冷恢复 ·
子运行再派子运行(含深度守卫)。

> ✅ 【2026-09-22 10:45 划掉一项】**`run store 保留策略` 不再是挂起项。**
> 用户拍板"自运行时单独日志,**要留日志**",并给出隔离诉求:「最先的诉求是**日志要隔离开**,
> 同族的就放一起,不要同目录等级下有不同会话的主日志又有各自的 subagent」。
> → 轨迹仓取**卡的目录** `sessions/<sid>/subruns/`,于是它**跟卡同生共死**:
> `store.delete_session()` 把整袋移进 `archive/<ts>-<sid>/`,不需要另立保留规则,
> 也不会在 `data/` 下积一个只增不减的目录。**"留多久、谁清"这个问题自动消失了。**
> 接线落点:见 §9 与 `server/app/engine.py` 的 `_subrun_store_for()`。

---

## 9. 代码落点现状(2026-09-16 用户批准毕业)

| 路径 | 状态 |
|---|---|
| `core/subrun.py` | **执行器(已毕业进内核)** —— 只 import core 四个原语 |
| `server/app/worker_tools.py` | **工人的手(已毕业进产品层)** —— 四个只读工具 + 沙箱 + 错误池;文件工具因沙箱根未拍不接线。⚠️ **本文件只剩实现** —— 模型可见文案(4 个 `description` + 4 个 `usage` + 11 条参数说明)2026-09-22 已归位到 `character/tools.py` 的 `WORKER_*` 常量(见下面"文案归位"一条) |
| `character/tools.py` | 委派工具文案 + 工厂(能力句由工人工具集**生成**)+ **工人工具的全部模型可见文案**(`WORKER_*`) |
| `character/personas.py` | `SUBAGENT_SYSTEM`(工人的任务说明)、`SUBAGENT_BUDGET_TEMPLATE`(步数预算) |
| `server/app/engine.py` | 装配 + 接线(runner / 能力表 / 晚绑定 llm / **子运行轨迹仓**) |
| `server/params.py` | 三个 `SUBAGENT_*` 参数,全 ⏳ |
| `test/lab/scheduler.py` | 队列(**仍未收口,未毕业**) |
| `test/subrun_probe.py` | 探针:`py test/subrun_probe.py [--real]` |
| `test/subagent_prompt_lab.py` | 提示词 A/B 实验台(真模型,出委派率表) |
| `test/subagent_prompt_view.py` | 提示词视图(离线,看文案落在哪) |
| `test/test_subrun.py` / `test/test_worker_tools.py` / `test/test_subagent_wiring.py` | 自测 |

**毕业时定下来的三条装配约束**(实测,见 `test/test_subagent_wiring.py`):
工具在模块加载时注册一次 + llm 晚绑定;注册必须早于 `_loop` 构造(retain 快照);
子运行 SYSTEM 走每轮覆盖,绝不把 `sys_by_source` 递进去(否则穿上她的人设)。

### 9.1 ✅ 子运行轨迹落盘(2026-09-22 接线)

**落点**:`sessions/<sid>/subruns/<run_id>.jsonl`(一跑一个)。
装配处 = `server/app/engine.py` 的 `_subrun_store_for(sid)` 与 `_sid_now()`;
路径由 `server/store.py` 的 `subruns_dir(sid)` 算(与 `images_dir` 同款)。

**用户拍板原话**:
> 「自运行时单独日志,**要留日志**」「我在想让一个会话里的配置要不要都放同一个目录……
>  但可能动的刀子比较大,所以最先的诉求是**日志要隔离开,同族的就放一起**,
>  不要同目录等级下有不同会话的主日志又有各自的 subagent,管理和回看会很乱」

**三条直接结论**:
1. 一张卡的**全部日志同族同处**:`chat.log`(她和你)+ `subruns/*.jsonl`(工人)
   + `meta.json`(快照)+ `images/` 都在 `sessions/<sid>/` 一层里;
2. **不另开 `data/subruns/` 那种全局平铺目录**(那正是"乱"的定义:
   所有卡的工人日志挤在一层,只能靠 run_id 猜是哪张卡的);
3. **清理策略不用另立** —— 跟卡同生共死(见 §8 的更正块)。

**同时接了 `parent`**(血缘 = 派活那一刻正在跑的那张卡)。两者是"一件事的两半":
没有 store,血缘 id 记了也没地方查;没有 parent,有了文件也不知道是谁的。

⚠️ sid 拿不到时(工人在实验台/探针里跑)**不落盘** —— 宁可不写,
也不要写到一个猜出来的卡目录里。所以探针仍然走 `store=STORE`/`store=None` 的老路。

**仍未做**(属用户"刀子比较大"的那部分,别自己动):一个会话的**配置**是否也收进
同一目录 + **快照统一 yaml 管理**(现在是 `meta.json`,预设仍是全局的)。
用户原话:"可能动的刀子比较大" —— 所以本轮**只做了日志隔离**。

### 9.2 ✅ 工人工具文案归位(2026-09-22)

**`description` / `usage` / 参数说明一律住内容层**(`character/tools.py` 的 `WORKER_*`),
`server/app/worker_tools.py` 只 import。15 条常量,搬移**逐字未改**
(对拍:同一 `make_read_only_tools(root=".")` 在改前/改后渲染出的
`description`/`usage`/参数说明**完全相同**)。

**依据是已有的成文规矩**(用户 2026-09-22 提醒"我记得是一种工具分层思想的"时确认):
- `docs/README.md`「内容层文案 = `character/personas.py` + `character/tools.py`
  (**每个工具的 `description` / `usage` —— 也是内容层,别漏**)」;
- `docs/decisions/TRAPS.md` 二.2「**内容层文案只有一处来源**」(手抄多份必然漂移);
- 判例:`WAKE_BUDGET_TEMPLATE` 曾写死在 producer 里 → 归位到 `personas.py`。

⚠️ **"工具分层"与"文案归内容层"是两件事,别混**:
前者说的是**工具分两层**(她的工具 ↔ 工人的手,判据见 §3.3 四条轴);
后者说的是**文案住哪**(内容层)。`worker_tools.py` 作为"工人的手"
**继续留在产品层**(网络与文件 IO,不是纯逻辑,见文件头)—— 变的只是文案的来源。

**仍未收口**(§7 六条 + 失败契约)不影响已落地的部分 —— 契约变的是结算语义,
不是这三个位置。

> **【2026-09-23 00:01 补:归位 ≠ 生效】**
> 上面那次归位让 `description` / `usage` / 参数说明**都住对了地方** —— 但当时
> **只有 `description` 与参数说明真的到达了模型**:
> `description` / 参数说明走 `tools[]` 数组(与 SYSTEM 无关,照常到达);
> 而 **`usage` 走 `[可用工具用法]` 段,那段从未被渲染过** —— 工人的 SYSTEM
> 是一条静态串,composer 整条不跑(五天,零症状)。所以那 2 条**已接线工具**的
> `usage` 是"住对了地方、但够不着模型";另 2 条(`list_files` / `read_text_file`)
> 则连 `description` 都到不了 —— **因为工具本身没接线**(等 `SUBAGENT_FILE_ROOT`)。
> 现已修(甲A,见 §4.1 / §4.2 与 `docs/decisions/TIMELINE.md`「2026-09-23 00:01」)。
> **教训:"文案归位"和"文案生效"是两件事,验收时必须分别给证据。**

