# AI 协作边界与矛盾登记(AI-GUARDRAILS)

> 性质:**给 AI 自己看的守则 + 每次跟"我(用户)"闹矛盾的登记**。放根目录,
> 每次接手任务先读。用户不想反复掰扯同一类问题 —— 这里把"别再犯"钉死。
> 状态图例见 `docs/README.md`。2026-09 立。

---

## 一、用户反复拍板过的硬边界(不许再踩)

以下任何一条被违反,都导致用户浪费时间重跑 —— 是本文件存在的全部理由。

| # | 边界(用户原话或要点) | 违反过的实例 |
|---|---|---|
| 1 | **内核 `core/` 是纯逻辑,不许为显示/实验台加钩子**。"谁准你往内核里加钩子了??就他妈改一下 lab" | a3b9682 给 `core/loop.py` 加 `on_input` 钩子 → 被 revert |
| 2 | **lab 试出来的现象默认只留 prompt_lab,别推进产品执行路径**(心跳/脉冲/engine)。产品语义要用户明确拍板 | c6afb9f 把 lab"当前时间=事件start"推进 LifeLoop/pulse → d665a3a 回退 |
| 3 | **人设文案在 `character/personas.py`(内容层);引擎只装配**;改人设改 personas,不是 server/UI;别把文案写死在装配代码里 | [时间预算] 句子曾写死在 engine producer → 归位 personas |
| 4 | **`server/params.py` = 产品参数唯一来源**,✅拍板/⏳待拍;未拍的一律 ⏳,"禁止当已定使用",不许我给默认 | — |
| 5 | **普通轮(自走/心跳/脉冲)与补写是同一 LifeSampler 事件算法**,只差触发点;预算锚 `[日志尾→当前]`,兜底 start+预算≤当前 | 612ae32 用浮空 `draw_budget_min` → a0c7d9e 改回锚 LifeSampler |
| 6 | **"该轮不触发任何事件就结束"**(无事件 = 安静,不调 LLM) | 文档写了但代码没落地 → 9df2233 才拍板落地 |
| 7 | **自走轮只有一个算法;情境文案补写复用 SELF_SITUATION,不写第二份** | 曾并存 SELF/BACKFILL_SITUATION → 删 |
| 8 | **[时间线] 只挂独处轮(自走/补写),不挂陪聊轮**(正在聊天时是噪音) | chat composer 挂过 timeline → 41fce48 撤 |
| 9 | **改动前先把用户的算法原文逐字复述出来**再动手,用他的词,不转述 | —(本守则本身由多起"漏接原话"总结) |
| 10 | **碰 core / server 产品执行路径前,先报"动哪层、为什么",等点头** | 本会话多次先斩后奏 |

---

## 二、AI 自身矛盾登记(时间 · 我做了什么 · 错在哪 · 用户怎么纠正 · 守则)

> 记的是"我"的错,不是用户的。用户是产品 owner,他拍板语义;我做显示/实现。

| 时间(约) | 我做了什么 | 错在哪(自我归因) | 你的纠正 | 守则 |
|---|---|---|---|---|
| 2026-09-07 | 普通轮预算用浮空随机抽数 | 没照"与补写同一事件算法",图省事抽一个数 | "预算呢…不是浮空抽数" | 改前复述你的算法原文 |
| 2026-09-07 | 为"每次 LLM 调用都打输入"给 `core/loop.py` 加 on_input 钩子 | 显示问题不该进内核;越层 | "谁准你往内核里加钩子" | 显示/调试在 lab/装配层包代理,core 零改动 |
| 2026-09-07 | chat 陪聊轮也挂 [时间线] | 机制按"它看着像"外扩,没守它的意图域 | "正常聊天轮你组时间线" | 铺机制前先确认它属于哪类轮 |
| 2026-09-07 | lab 强制轮把 start 随机数抽完就丢、世界钟钉注入时刻 | 漏接你"区间随机 start + 预算、当前时间=start";没先排出"该看到的结果" | "两个随机数…另一个哪去了" | 实现前先列出这轮 SYSTEM 该长什么样 |
| 2026-09-07 | 把 lab 现象推进产品 LifeLoop/pulse(锚事件start) | lab 是测试仪器,不是产品逻辑;我把 demo 当 bug | "只是拿来测输入输出…和实际产品有什么关系" | lab 现象只留 lab;产品语义要你拍 |
| 2026-09-07 | 把没经你拍的范围写进 MAP 当"拍板"(产品锚定那条) | 记录说谎 → 后续被自己的错误误导 | "文档里有矛盾的东西作祟" | MAP 拍板只记你原话,推断要标"待确认" |
| 2026-09-07 | 无事件自走轮仍硬跑 LLM 产碎碎念 | 文档写"安静结束"但代码没实现 | "预算呢…该轮不触发就结束" | 文档语义 = 代码行为,先对齐再交付 |

**共性根因(对 AI 自己说)**:默认从"手头实现 + 最近一句指令"找自洽改动,而不从
"用户拍板源 + 该功能的意图域 + 分层边界"重推 → 表现成 ① 爱外扩(越范围)② 漏
原话(重构而非照抄)③ 记录自己写的错再当真理。治法是上表"守则"列 + 本文 §三。

---

## 三、协作守则(每次开工先过一遍)

1. **复述原文**:凡是改行为/语义前,先把用户那条算法/指令逐字写在改动注释或回复里,确认是"他的词"不是"我的转述"。
2. **报层再动**:lab/core/server(产品)/character(文案)/params(参数)—— 先说是哪一层、动什么、为什么;core 与产品执行路径没有用户点头不动。
3. **排结果**:改动前先写出"用户将看到/运行后的结果长什么样"(如 SYSTEM 布局),自己先对一遍他要不要这个。
4. **未拍 = ⏳**:不给默认,不把"沿用值"当已定;params/文档里 ⏳ 就是没拍。
5. **不污染记录**:MAP/拍板只写用户原话;我推断的范围标"(待确认)"。发现旧文档自相矛盾 → 进 `docs/pitfalls/` + 报给用户,不默默二选一。
6. **小步可验**:每个改动跑对应测试 + 冒烟;别把"我以为"当"我验过"。

---

## 四、对用户的提醒(这篇不只记我的错)

- 你有几处指令是在**看到我产出后**才补全的(如"当前时间=start"是看到强制轮输出才说透)。
  这没问题,但意味着:我按你**上一条**做时,往往还没拿到**这一条** —— 所以重要语义
  你尽早给我,能少一轮返工。
- 文档(尤其 STRUCTURE)有**陈旧矛盾**(git init/_life/行数/测试数),正在逐份拆/清;
  在拆完前,以 **MAP 最新拍板 + params.py ✅/⏳ + personas.py 内容层** 为当前真值。
